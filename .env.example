# API service layer — when set, src/services/api.ts dispatches to the real REST API
# instead of the local Zustand mocks. Leave empty for full mock mode.
VITE_API_BASE_URL=

# Supabase (used once auth is wired)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
